<h1> Edit comment</h1>
<%= form_with model: @comment, url: post_comment_path(params[:post_id]) do |f| %>
    <div>
        <%= f.label :body %>
        <%= f.text_area :body %>
    </div>

    <div>
        <%= f.submit %>
    </div> 
<% end %>
<%= link_to 'Back', posts_path %>