<h1>Post</h1>

<div>
    <%= @post.title%>
</div>

 <div>
    <%= @post.body%>
</div>

 <div>
    <%= @post.author%>  
</div>

<%= form_with model: post.comments.build do |f| %>
    <div>
        <%= f.label :body %>
        <%= f.text_area :body %>
    </div>
    <div>
        <%= f.submit %>
    <div>        


<%= link_to 'Back', posts_path%>

<%= link_to 'Delete', to: '#'%>